import { IconMugFilled } from "@tabler/icons-react";

function Header() {
    return (
        <header className="header">
            <h1>Cafe Shop</h1>
            <IconMugFilled />
        </header>
    );
}

export default Header;